export default class GameState {
  static gameStats = {
    wins: 0,
  };

  constructor() {
    this.state = {
      currentTurn: {

      },
    };
  }
  static from(object) {
    // TODO: create object
    return null;
  }
}
